# BelT (СТБ 34.101.31-2011): режимы работы

Спека для реализации режимов в `lab2/src/encryption/bel_t.rs`.
Источник — `lab2/belt-spec27.pdf` (СТБ 34.101.31-2011, «Информационные технологии
и безопасность. Алгоритмы шифрования и контроля целостности данных», введён
2011-07-01).

Разделы стандарта:

| Пункт | Алгоритм | Статус в lab2 |
|---|---|---|
| 6.1 | блочное преобразование `F` / `F⁻¹` | сделано, сверено с А.1/А.2 |
| 6.2 | `belt-ecb` — простая замена | сделано, но **не по стандарту** (см. §3) |
| 6.3 | `belt-cbc` — сцепление блоков | не входит в задание |
| 6.4 | `belt-cfb` — гаммирование с обратной связью (`CFM`) | **делать**, §5 |
| 6.5 | `belt-ctr` — счётчик (`CTR`) | **делать**, §4 |
| 6.6 | `belt-mac` — имитовставка | опционально, §6 |

Все формулы ниже проверены на контрольных примерах приложения А (§7): собирался
отдельный бинарник из копии `bel_t.rs` + реализации режимов, все 11 векторов
совпали, round-trip проходит на всех длинах 0..80 байт.

> Замечание про PDF: шрифты в нём без ToUnicode-карт, поэтому `pdftotext`
> выбрасывает кириллицу (латиница, цифры и hex-таблицы извлекаются нормально).
> Формулы читать глазами в просмотрщике; hex-векторы удобно вытаскивать через
> `pdftotext -layout belt-spec27.pdf -`.

## 1. Общие соглашения

| | |
|---|---|
| Блок | 16 байт (128 бит) |
| Байты ↔ число | little-endian: `u128::from_le_bytes` / `to_le_bytes` (уже есть `BelT::block_from`) |
| `F(·)` | `BelT::encrypt_block` — зашифрование блока (п. 6.1.3) |
| `F⁻¹(·)` | `BelT::decrypt_block` — расшифрование блока (п. 6.1.4) |
| Ключ | `Key<8>` = 8 слов `u32`, слово `i` — байты `4i..4i+4` ключа в little-endian |
| `Left_r(u)` | первые `r` байт блока `u` |
| `⟨1⟩₁₂₈` | единица как 128-битное число, т.е. `wrapping_add(1)` над `u128` |

Ключ в `common::cli::parse_key` собирается через `u32::from_le_bytes`, поэтому
hex-ключ из стандарта вводится в CLI как есть, байт за байтом:
`E9DEE72C8F0C0FA62DDB49F46F73964706075316ED247A3739CBA38303A98BF6`.

**В обоих гамма-режимах (`CTR`, `CFM`) `F⁻¹` не используется вообще** — и при
зашифровании, и при расшифровании вызывается только `encrypt_block`.

### Формат шифртекста (наша договорённость, не стандарт)

Стандарт считает синхропосылку `S` внешним параметром. В проекте (как в lab1) она
генерируется случайно при зашифровании и кладётся открытым текстом первым блоком:

```
шифртекст = S (16 байт) ‖ гамма-поток (|X| байт)
```

Это уже заложено в `lab2/src/main.rs`: для `CTR` и `CFM` назначен
`cli::check_sync_prefix`, который перед расшифрованием проверяет `len ≥ 16`.
Дополнения (паддинга) в этих режимах нет, длина сохраняется:
`|Y| = 16 + |X|`. Ограничения снизу на `|X|` нет (п. 6.4.1, 6.5.1: только
`|Xₙ| ≤ 128` бит), последний неполный блок гаммируется по фактической длине.

Требование к `S`: **не должна повторяться при одном и том же ключе**. Для `CTR`
повтор катастрофичен (XOR двух шифртекстов = XOR открытых текстов), для `CFM` тоже
нежелателен. 128 случайных бит на сообщение это обеспечивают.

Генерация (`rand 0.10` уже в `lab2/Cargo.toml`, как в lab1):

```rust
use rand::RngExt;

let sync: u128 = rand::rng().random();
```

## 2. Что уже сделано и верно

Блочное преобразование (п. 6.1) реализовано корректно: `H`-таблица, `G_r`
(байты через `H`, затем `rotate_left` над LE-словом), порядок девяти операций
такта с `e = ⟨i⟩₃₂ ⊕ G21(b + c + K₇ᵢ₋₃)`, развёртка ключа `K[(7i-k) mod 8]`,
финальные перестановки `Y ← b‖d‖a‖c` (зашифрование) и `X ← c‖a‖d‖b`
(расшифрование), обратный порядок подключей в `decrypt_block`.

Сверено с А.1 и А.2 — совпадает побитово.

## 3. Режим простой замены, п. 6.2 (`belt-ecb`)

### Расхождение текущей реализации со стандартом

Сейчас в `encrypt_ecb`: дополнение нулями до кратности 16 + байт с числом добитых
байт в конец шифртекста, `|Y| = |X| + pad + 1`. Схема обратима и round-trip
проходит, но стандарту не соответствует.

Стандарт (п. 6.2.1–6.2.3): `|X| ≥ 128` бит, дополнения нулями нет, длина
сохраняется (`|Y| = |X|`), неполный последний блок обрабатывается **кражей
шифртекста** (ciphertext stealing) — недостающие биты берутся из предыдущего
блока шифртекста:

```
Если |Xₙ| = 128:   Yᵢ ← F(Xᵢ),  i = 1..n
Иначе (0 < r = |Xₙ| < 128):
    Yᵢ      ← F(Xᵢ),  i = 1..n-2
    Y*      ← F(X_{n-1})
    Yₙ      ← Left_r(Y*)
    Y_{n-1} ← F( Xₙ ‖ (последние 16-r байт Y*) )
```

Хорошо видно на векторах: в А.7 (`|X| = 376` бит) последние 15 байт шифртекста —
это первые 15 байт того блока, который в А.6 (`|X| = 384`) стоит вторым.
Расшифрование (п. 6.2.4) — тот же алгоритм с `F⁻¹`, перестановка симметрична.

### Реализация CTS (проверена на А.6–А.9)

```rust
fn apply_ecb(mut bytes: Vec<u8>, key: &Key<8>, encrypt: bool) -> Vec<u8> {
    let full = bytes.len() / BLOCK_SIZE;   // число полных блоков
    let rest = bytes.len() % BLOCK_SIZE;   // |X_n|, если последний блок неполный

    let apply = |n: u128| {
        if encrypt { Self::encrypt_block(n, key) } else { Self::decrypt_block(n, key) }
    };

    for i in 0..full {
        let processed = apply(Self::block_from(&bytes[i * BLOCK_SIZE..][..BLOCK_SIZE]));
        bytes[i * BLOCK_SIZE..][..BLOCK_SIZE].copy_from_slice(&processed.to_le_bytes());
    }

    if rest != 0 {
        let last = (full - 1) * BLOCK_SIZE;         // блок n-1
        for j in 0..rest {
            bytes.swap(last + j, last + BLOCK_SIZE + j);   // кража шифртекста
        }
        let processed = apply(Self::block_from(&bytes[last..][..BLOCK_SIZE]));
        bytes[last..][..BLOCK_SIZE].copy_from_slice(&processed.to_le_bytes());
    }

    return bytes;
}
```

`encrypt_ecb` / `decrypt_ecb` — это `apply_ecb(bytes, key, true/false)`.

### Что придётся тронуть при переходе на CTS

* `common::cli::check_ecb_padding` заменяется на проверку «длина ≥ 16 байт»
  (байта паддинга больше нет).
* Появляется требование `|X| ≥ 16` уже **при зашифровании**, а `CipherFn`
  возвращает `Vec<u8>` без `Result`. Чисто вписывается симметрично существующему
  дизайну: добавить в `CipherMode` поле `check_plaintext: Option<CheckFn>` и
  проверять его в `run_cipher` перед вызовом `encrypt` (рядом с уже имеющейся
  проверкой `check_ciphertext`).
* Старые файлы `*.belt`, зашифрованные текущей схемой с паддингом, читаться
  перестанут.

**Решение не принято.** Альтернатива — оставить нули + байт длины как учебное
упрощение и явно написать в отчёте, что это отступление от п. 6.2. Тогда
ECB-часть кода не меняется вообще.

## 4. Режим счётчика, п. 6.5 (`CTR`)

```
s ← F(S)
для i = 1..n:
    s  ← s + ⟨1⟩₁₂₈          (mod 2¹²⁸, счётчик — LE-число)
    Yᵢ ← Xᵢ ⊕ Left_{|Xᵢ|}(F(s))
```

Расшифрование (п. 6.5.4) — **та же процедура**. Важные детали: счётчик
инкрементируется *до* первого гаммирования (первая гамма — `F(F(S) + 1)`, а не
`F(F(S))`), и шифруется значение счётчика, а не предыдущая гамма.

```rust
fn apply_ctr(bytes: &[u8], key: &Key<8>, sync: u128) -> Vec<u8> {
    let mut counter = Self::encrypt_block(sync, key);
    let mut result = Vec::with_capacity(bytes.len());

    for block in bytes.chunks(BLOCK_SIZE) {
        counter = counter.wrapping_add(1);
        let gamma = Self::encrypt_block(counter, key).to_le_bytes();
        result.extend(block.iter().zip(gamma).map(|(byte, g)| byte ^ g));
    }

    return result;
}

pub fn encrypt_ctr(plain_bytes: Vec<u8>, key: &Key<8>) -> Vec<u8> {
    let sync: u128 = rand::rng().random();

    let mut result = Vec::with_capacity(BLOCK_SIZE + plain_bytes.len());
    result.extend_from_slice(&sync.to_le_bytes());
    result.extend_from_slice(&Self::apply_ctr(&plain_bytes, key, sync));

    return result;
}

pub fn decrypt_ctr(encrypted_bytes: Vec<u8>, key: &Key<8>) -> Vec<u8> {
    // len >= BLOCK_SIZE гарантирует cli::check_sync_prefix
    let (sync, body) = encrypted_bytes.split_at(BLOCK_SIZE);

    return Self::apply_ctr(body, key, Self::block_from(sync));
}
```

Свойства для отчёта: длина сохраняется; блоки независимы (распараллеливание,
произвольный доступ к блокам); одиночная ошибка в бите шифртекста портит ровно тот
же бит открытого текста — размножения ошибок нет; одинаковые блоки открытого
текста дают разные блоки шифртекста.

## 5. Режим гаммирования с обратной связью, п. 6.4 (`CFM`)

Регистр обратной связи — предыдущий блок **шифртекста**:

```
Y₀ ← S
Зашифрование:   Yᵢ ← Xᵢ ⊕ Left_{|Xᵢ|}(F(Y_{i-1}))
Расшифрование:  Xᵢ ← Yᵢ ⊕ Left_{|Yᵢ|}(F(Y_{i-1}))
```

Единственная разница между направлениями — что уходит в регистр: при
зашифровании результат XOR, при расшифровании входной блок. Неполный блок всегда
последний, регистр после него уже не нужен.

```rust
fn apply_cfm(bytes: &[u8], key: &Key<8>, sync: u128, encrypt: bool) -> Vec<u8> {
    let mut feedback = sync;
    let mut result = Vec::with_capacity(bytes.len());

    for block in bytes.chunks(BLOCK_SIZE) {
        let gamma = Self::encrypt_block(feedback, key).to_le_bytes();
        let processed: Vec<u8> = block.iter().zip(gamma).map(|(byte, g)| byte ^ g).collect();

        let cipher_block = if encrypt { &processed[..] } else { block };
        if cipher_block.len() == BLOCK_SIZE {
            feedback = Self::block_from(cipher_block);
        }

        result.extend_from_slice(&processed);
    }

    return result;
}
```

Обвязка `encrypt_cfm` / `decrypt_cfm` — такая же, как у `CTR` (§4): генерация `S`,
префикс, `split_at(BLOCK_SIZE)`, только с флагом `encrypt: true/false`.

Свойства для отчёта: длина сохраняется; зашифрование строго последовательное,
расшифрование распараллеливаемо; ошибка в блоке `Yᵢ` портит соответствующие биты
`Xᵢ` **и весь блок `X_{i+1}` целиком**, дальше поток восстанавливается —
самосинхронизация через один блок. Это главное отличие от `CTR`.

## 6. Имитовставка, п. 6.6 (`belt-mac`) — опционально

В `lab2/src/main.rs` сейчас `mac: None`. Если MAC нужен (в lab1 он был):

```
r ← F(0¹²⁸),  s ← 0¹²⁸
для i = 1..n-1:  s ← F(s ⊕ Xᵢ)
если |Xₙ| = 128:  s ← F(s ⊕ Xₙ ⊕ Ψ1(r))
иначе:            s ← F(s ⊕ (Xₙ ‖ 0x80 ‖ 0…0) ⊕ Ψ2(r))
имитовставка ← Left_8(s)          // 64 бита
```

где по 32-битным словам `u = u₁‖u₂‖u₃‖u₄`:

```
Ψ1(u) = u₂ ‖ u₃ ‖ u₄ ‖ (u₁ ⊕ u₂)
Ψ2(u) = (u₁ ⊕ u₄) ‖ u₁ ‖ u₂ ‖ u₃
```

Дополнение `Xₙ ‖ 1 ‖ 0…0` на байтовых данных — это просто добавленный байт `0x80`
и нули до 16 байт.

Проверено на А.17 и А.18. Учесть: имитовставка 64-битная, а
`common::cli::MacFn` возвращает `Option<u32>` и `MacSpec::max_bits` рассчитан на
32 бита — под BelT нужно расширять до `u64` (затронет lab1: там MAC 32-битный,
`u64` его тоже вмещает, поменяется только формат печати).

## 7. Контрольные примеры (приложение А)

Все значения — байтовые строки в hex, пробелы для читаемости. `K` вводится в CLI
как есть.

```
K1 = E9DEE72C 8F0C0FA6 2DDB49F4 6F739647 06075316 ED247A37 39CBA383 03A98BF6
K2 = 92BD9B1C E5D14101 5445FBC9 5E4D0EF2 682080AA 227D642F 2687F934 90405511
S1 = BE329713 43FC9A48 A02A885F 194B09A1
S2 = 7ECDA4D0 1544AF8C A58450BF 66D2E88A
```

### А.1 — `F` (один блок), ключ K1
```
X = B194BAC8 0A08F53B 366D008E 584A5DE4
Y = 69CCA1C9 3557C9E3 D66BC3E0 FA88FA6E
```

### А.2 — `F⁻¹` (один блок), ключ K2
```
Y = E12BDC1A E28257EC 703FCCF0 95EE8DF1
X = 0DC53006 00CAB840 B38448E5 E993F421
```

### А.6 — belt-ecb, зашифрование, `|X| = 384` бита (48 байт), ключ K1
```
X = B194BAC8 0A08F53B 366D008E 584A5DE4 8504FA9D 1BB6C7AC 252E72C2 02FDCE0D
    5BE3D612 17B96181 FE6786AD 716B890B
Y = 69CCA1C9 3557C9E3 D66BC3E0 FA88FA6E 5F23102E F1097107 75017F73 806DA9DC
    46FB2ED2 CE771F26 DCB5E5D1 569F9AB0
```

### А.7 — belt-ecb, зашифрование, `|X| = 376` бит (47 байт), ключ K1 — случай CTS
```
X = B194BAC8 0A08F53B 366D008E 584A5DE4 8504FA9D 1BB6C7AC 252E72C2 02FDCE0D
    5BE3D612 17B96181 FE6786AD 716B89
Y = 69CCA1C9 3557C9E3 D66BC3E0 FA88FA6E 36F00CFE D6D1CA14 98C12798 F4BEB207
    5F23102E F1097107 75017F73 806DA9
```

### А.8 — belt-ecb, расшифрование, `|Y| = 384` бита, ключ K2
```
Y = E12BDC1A E28257EC 703FCCF0 95EE8DF1 C1AB7638 9FE678CA F7C6F860 D5BB9C4F
    F33C657B 637C306A DD4EA779 9EB23D31
X = 0DC53006 00CAB840 B38448E5 E993F421 E55A239F 2AB5C5D5 FDB6E81B 40938E2A
    54120CA3 E6E19C7A D750FC35 31DAEAB7
```

### А.9 — belt-ecb, расшифрование, `|Y| = 288` бит (36 байт), ключ K2 — случай CTS
```
Y = E12BDC1A E28257EC 703FCCF0 95EE8DF1 C1AB7638 9FE678CA F7C6F860 D5BB9C4F
    F33C657B
X = 0DC53006 00CAB840 B38448E5 E993F421 5780A6E2 B69EAFBB 258726D7 B6718523
    E55A239F
```

### А.14 — belt-cfb, зашифрование, ключ K1, синхропосылка S1
```
X = B194BAC8 0A08F53B 366D008E 584A5DE4 8504FA9D 1BB6C7AC 252E72C2 02FDCE0D
    5BE3D612 17B96181 FE6786AD 716B890B
Y = C31E490A 90EFA374 626CC99E 4B7B8540 A6E48685 464A5A06 849C9CA7 69A1B0AE
    55C2CC59 39303EC8 32DD2FE1 6C8E5A1B
```

### А.15 — belt-cfb, расшифрование, ключ K2, синхропосылка S2
```
Y = E12BDC1A E28257EC 703FCCF0 95EE8DF1 C1AB7638 9FE678CA F7C6F860 D5BB9C4F
    F33C657B 637C306A DD4EA779 9EB23D31
X = FA9D107A 86F375EE 65CD1DB8 81224BD0 16AFF814 938ED39B 3361ABB0 BF0851B6
    52244EB0 6842DD4C 94AA4500 774E40BB
```

### А.16 — belt-ctr, ключ K1, синхропосылка S1
```
X = B194BAC8 0A08F53B 366D008E 584A5DE4 8504FA9D 1BB6C7AC 252E72C2 02FDCE0D
    5BE3D612 17B96181 FE6786AD 716B890B
Y = 52C9AF96 FF50F644 35FC43DE F56BD797 D5B5B1FF 79FB4125 7AB9CDF6 E63E81F8
    F0034147 3EAE4098 33622DE0 5213773A
```

### А.17, А.18 — belt-mac, ключ K1
```
X    = B194BAC8 0A08F53B 366D008E 58            (13 байт)
MAC  = 7260DA60 138F96C9

X    = B194BAC8 0A08F53B 366D008E 584A5DE4 8504FA9D 1BB6C7AC 252E72C2 02FDCE0D
       5BE3D612 17B96181 FE6786AD 716B890B      (48 байт)
MAC  = 2DAB5977 1B4B16D0
```

Векторы для `belt-cbc` (п. 6.3) — таблицы А.10–А.13 в PDF, если режим
понадобится.

## 8. Как проверять

Тестовые данные в таблицах §7 — это байтовые строки, а `CipherFn` работает с
`Vec<u8>`, поэтому векторы проверяются прямым вызовом функций режима, а не через
CLI (CLI сам генерирует `S` и добавляет префикс). Минимальный набор проверок:

1. **KAT из §7** — по вектору на каждый режим; для `CTR`/`CFM` вызывать
   внутренние `apply_ctr` / `apply_cfm` с фиксированной `S`, минуя генерацию
   случайной.
2. **Round-trip** на всех длинах `0..80` байт: `decrypt(encrypt(m)) == m` для
   `CTR` и `CFM`; для ECB с CTS — начиная с 16 байт.
3. **Размножение ошибки** (для отчёта): испортить байт в блоке `i` шифртекста —
   в `CTR` портится ровно один байт открытого текста, в `CFM` — этот байт и весь
   блок `i+1`.
4. **Разные `S`** дают разный шифртекст для одного и того же открытого текста.

Удобно оформить как `#[cfg(test)] mod tests` внутри `bel_t.rs` — приватные
`encrypt_block` / `apply_ctr` оттуда доступны.

## 9. Открытые решения

1. **ECB**: переходить на CTS по п. 6.2 (правки в `common`, старые `.belt`-файлы
   ломаются) или оставить нули + байт длины как учебное упрощение с оговоркой в
   отчёте.
2. **Объём**: только `CTR` + `CFM`, или ещё `belt-cbc` (п. 6.3) и `belt-mac`
   (п. 6.6, требует расширения `MacFn` до `u64`).
