#import "@local/stp2024:0.1.0"
#include "title.typ"

#show: stp2024.template

#stp2024.full_outline()
#pagebreak()

#let belt_source = read("../src/encryption/bel_t.rs")
#let belt_parts = belt_source.split("#[cfg(test)]")

= Цель работы
Целью данной лабораторной работы является изучение принципов построения
симметричных блочных шифров на примере белорусского стандарта шифрования
СТБ 34.101.31-2011, определяющего алгоритм BelT. Задачи включают изучение
структуры блочного преобразования, положенного в основу стандарта,
проектирование и реализацию программного средства, осуществляющего шифрование
и дешифрование данных в режимах простой замены и гаммирования с обратной
связью, а также генерацию имитовставки и тестирование разработанного
программного средства.

Конечным результатом данной лабораторной работы должна быть разработанная
программа, осуществляющая шифрование и дешифрование данных по
СТБ 34.101.31-2011 во всех перечисленных режимах как для текста, вводимого с
клавиатуры, так и для файлов произвольного содержания, и вычисляющая
имитовставку заданной длины.

= Ход работы
В ходе выполнения работы было создано программное средство на языке
программирования _Rust_. Реализация алгоритма BelT вынесена в отдельный модуль
`encryption::bel_t`, взаимодействие с пользователем осуществляется через
текстовое меню, позволяющее выбрать источник данных (текст или файл),
выполняемую операцию (зашифрование, расшифрование или вычисление
имитовставки), режим работы алгоритма и 256-битный ключ. Помимо
предусмотренных заданием режимов простой замены и гаммирования с обратной
связью дополнительно реализованы режимы сцепления блоков и счётчика,
описанные тем же стандартом. Общий вид меню программы представлен на
рисунке @menu.

#figure(
  caption: [Меню программы],
  image(width: 85%, "img/menu.png"),
) <menu>

Алгоритм обрабатывает блок длиной 128 бит, представленный четырьмя 32-битными
словами, и использует ключ длиной 256 бит, разделённый на восемь 32-битных
подключей. Преобразование блока состоит из восьми тактов, в каждом из которых
выполняется семь обращений к подключам, поэтому за полное преобразование
каждое слово ключа используется семь раз. Единственным нелинейным элементом
является таблица замены $H$, переводящая байт в байт; вспомогательная функция
$G_r$ применяет эту таблицу к каждому байту слова и циклически сдвигает
результат влево на $r$ бит, причём используются три величины сдвига: 5, 13
и 21. В отличие от сети Фейстеля за один такт изменяются все четыре слова
блока, чем и объясняется малое число тактов. Номер такта участвует в
вычислениях как раундовая константа, что исключает совпадение тактов между
собой.

Зашифрование и расшифрование реализованы как независимые операции: при
расшифровании такты выполняются в обратном порядке, подключи внутри такта
распределяются зеркально, а итоговая перестановка слов отличается от
применяемой при зашифровании. Блок-схема базового преобразования блока
приведена в приложении @block-scheme-listing.

Особенностью стандарта является отказ от дополнения сообщения до целого числа
блоков. В режимах простой замены и сцепления блоков неполный последний блок
обрабатывается приёмом кражи шифртекста, при котором недостающие байты
заимствуются у зашифрованного предпоследнего блока, а в режимах гаммирования
последний блок гаммы усекается по фактической длине. Благодаря этому длина
шифртекста совпадает с длиной открытого текста, а в режимах, использующих
синхропосылку, превышает её ровно на 16 байт.

Перед расшифрованием выполняется проверка шифротекста на соответствие
выбранному режиму: в режимах, использующих кражу шифртекста, длина данных не
может быть меньше одного блока, а в режимах, использующих синхропосылку,
шифротекст должен содержать шестнадцатибайтовую синхропосылку.

Корректность реализации подтверждена контрольными примерами приложения А
стандарта: для базового преобразования блока, для каждого из четырёх режимов
работы, включая случаи неполного последнего блока, и для имитовставки
результаты совпадают с приведёнными в стандарте побитово. Проверка выполнена
модульными тестами, входящими в состав программного средства.

Исходный код разработанного программного средства приведён в
приложении @appendix-listing: реализация алгоритма представлена в
листинге @belt_code, точка входа в программу -- в листинге @main_code.

== Режим простой замены
В режиме простой замены каждый 128-битный блок открытого текста преобразуется
независимо от остальных, что не требует синхропосылки, но сохраняет одинаковые
блоки шифротекста для одинаковых блоков открытого текста. Результат работы
программы в данном режиме представлен на рисунке @ecb: открытому тексту из
повторяющихся символов соответствует шифротекст, в котором один и тот же
шестнадцатибайтовый блок повторяется дважды. Длина шифротекста равна длине
открытого текста, поскольку дополнение не применяется.

#figure(
  caption: [Зашифрование и расшифрование в режиме простой замены],
  image(width: 85%, "img/ecb.png"),
) <ecb>

== Режим сцепления блоков
В режиме сцепления блоков каждый блок открытого текста перед зашифрованием
складывается по модулю два с предыдущим блоком шифротекста, а роль нулевого
блока играет синхропосылка. Благодаря этому одинаковым блокам открытого текста
соответствуют различные блоки шифротекста, а зашифрование становится строго
последовательным. Результат работы программы в данном режиме представлен на
рисунке @cbc.

#figure(
  caption: [Зашифрование и расшифрование в режиме сцепления блоков],
  image(width: 85%, "img/cbc.png"),
) <cbc>

== Режим счётчика
В режиме счётчика шифрование выполняется наложением на открытый текст гаммы,
вырабатываемой зашифрованием последовательных значений счётчика. Начальное
значение счётчика получается зашифрованием синхропосылки, что исключает
пересечение гаммы для близких синхропосылок. Синхропосылка вырабатывается
случайным образом при каждом зашифровании и передаётся в начале шифротекста.
Данный режим не требует дополнения сообщения до целого числа блоков и
исключает совпадение блоков шифротекста. Результат работы программы в данном
режиме представлен на рисунке @ctr.

#figure(
  caption: [Зашифрование и расшифрование в режиме счётчика],
  image(width: 85%, "img/ctr.png"),
) <ctr>

== Режим гаммирования с обратной связью
В режиме гаммирования с обратной связью первый блок гаммы вырабатывается
зашифрованием синхропосылки, а каждый последующий -- зашифрованием предыдущего
блока шифротекста. Благодаря этому одинаковым блокам открытого текста
соответствуют различные блоки шифротекста, а искажение одного блока влияет на
результат расшифрования последующих. Блочное расшифрование в данном режиме,
как и в режиме счётчика, не используется вовсе: гамма вырабатывается
зашифрованием и при зашифровании, и при расшифровании. Результат работы
программы в данном режиме представлен на рисунке @cfm.

#figure(
  caption: [Зашифрование и расшифрование в режиме гаммирования с обратной связью],
  image(width: 85%, "img/cfm.png"),
) <cfm>


Таким образом все реализованные режимы работы описываемого стандартом
СТБ 34.101.31-2011 алгоритма были изучены, реализованы и протестированны в
полном объёме как для текста, вводимого с клавиатуры, так и для файлов.

#pagebreak()

#stp2024.heading_unnumbered[Заключение]
В ходе выполнения лабораторной работы были изучены принципы построения
симметричных блочных шифров на примере алгоритма BelT, определённого
стандартом СТБ 34.101.31-2011. Было разработано программное средство на языке
_Rust_, реализующее шифрование и дешифрование данных в режимах простой замены
и гаммирования с обратной связью, а также дополнительно в режимах сцепления
блоков и счётчика, и генерацию имитовставки заданной длины. Зашифрование и
расшифрование выполняются как отдельные операции над текстом, вводимым с
клавиатуры, либо над файлами произвольного содержания.

Тестирование показало, что расшифрование во всех реализованных режимах
восстанавливает исходное сообщение без искажений, а результаты преобразований
совпадают с контрольными примерами приложения А стандарта, что подтверждает
корректность реализации алгоритма. Сравнение режимов показало, что режим
простой замены сохраняет структуру открытого текста и потому непригоден для
шифрования сообщений, содержащих повторяющиеся блоки, тогда как остальные
реализованные режимы лишены данного недостатка: в режиме гаммирования с
обратной связью каждый блок гаммы вырабатывается зашифрованием предыдущего
блока шифротекста, поэтому повторяющиеся блоки открытого текста не приводят к
повторениям в шифротексте.

Отдельно следует отметить отличия рассмотренного стандарта от изученного ранее
ГОСТ 28147-89: вдвое больший размер блока, фиксированная в тексте стандарта
таблица замены, существенно меньшее число тактов преобразования и отказ от
дополнения сообщения в пользу кражи шифртекста, благодаря которому длина
сообщения сохраняется во всех режимах.

Все задачи лабораторной работы выполнены в полном объёме.

#stp2024.appendix(
  type: [обязательное],
  title: [Листинг программного кода],
  label: <appendix-listing>,
)[
  #stp2024.listing[Реализация алгоритма BelT на _Rust_][
    #raw(belt_parts.at(0).trim(at: end) + "\n", block: true)
  ] <belt_code>

  #stp2024.listing[Точка входа в программу][
    #raw(read("../src/main.rs"), block: true)
  ] <main_code>
]

#stp2024.appendix(
  type: [обязательное],
  title: [Блок-схема алгоритма],
  label: <block-scheme-listing>,
)[
  #figure(
    caption: [Блок-схема алгоритма],
    image(width: 100%, "img/block_scheme.svg"),
  ) <block_scheme>
]
